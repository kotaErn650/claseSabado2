let carro ={
    marca : "marca",
    modelo: "2009",
    valor : 10000


}
for (auto in carro  ){
    console.log(auto);

}

for (auto in carro  ){
    console.log( `${auto} : ${carro[auto]}`);

}

// una funcion es un bloque de codigo para ser usado en cualquier parte del progrma